<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.one.doo.member.mapper.MemberMapper">

	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item='type' collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'C'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'W'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<resultMap type="com.one.doo.auth.domain.Auth" id="authMap">
		<result property="userid" column="userid" />
		<result property="auth" column="auth" />
	</resultMap>

	<resultMap type="com.one.doo.member.domain.Member"
		id="memberMap">
		<id property="userid" column="userid" />
		<result property="userid" column="userid" />
		<result property="userpw" column="userpw" />
		<result property="userName" column="username" />
		<result property="regDate" column="regdate" />
		<result property="updateDate" column="updatedate" />
		<result property="email" column="email" />
		<collection property="authList" resultMap="authMap">
		</collection>
	</resultMap>


	<select id="read" resultMap="memberMap">
		SELECT
		mem.userid, userpw, username, enabled, regdate, updatedate, auth
		FROM
		users mem LEFT OUTER JOIN userauths auth on mem.userid = auth.userid
		WHERE mem.userid = #{userid}
	</select>

	<select id="getList" resultMap="memberMap">
		<![CDATA[	
	SELECT 
	  mem.userid,  userpw, username, enabled, regdate, updatedate, auth
	FROM 
	  users mem LEFT OUTER JOIN userauths auth on mem.userid = auth.userid 
	ORDER BY regdate 
	]]>
	</select>

	<select id="getListWithCri" resultMap="memberMap">
		<![CDATA[
		SELECT mem.userid,  userpw, username, enabled, regdate, updatedate, auth 
		FROM (SELECT /* + INDEX_DESC(tbl_board pk board) */
					rownum rn, mem.userid,  userpw, username, enabled, regdate, updatedate, auth
					FROM 
	  	users mem LEFT OUTER JOIN userauths auth on mem.userid = auth.userid 
					WHERE 	]]>
		<include refid="criteria"></include>
					<![CDATA[
					rownum <= #{pageNum} * #{amount}
					)
					WHERE rn > (#{pageNum} -1) * #{amount}
					]]> 
	</select>

	<select id="getTotalCount" resultType="int">
		SELECT count(mem.userid)
		FROM users mem LEFT OUTER JOIN userauths auth on mem.userid =
		auth.userid
	</select>


	<insert id="insert">
		INSERT INTO users
		(userid, userpw, username, email)
		VALUES (#{userid},
		#{userpw},
		#{userName},
		#{email})
	</insert>

	<!-- id중복체크 쿼리 -->
	<select id="idcheck" parameterType="String" resultType="Integer">
		SELECT
		COUNT(*)
		FROM users
		WHERE userid = #{userid}
	</select>

</mapper>
